# 数据库表结构创建报告

**创建时间**：{{ now() }}
**目标服务器**：{{ server.name }}（{{ server.id }}）
**数据库**：{{ database_name | default('N/A') }}（{{ db_type | default('') | upper }}）

---

## 创建摘要

| 项目 | 值 |
|------|-----|
| 状态 | {% if success %}✅ 成功{% else %}❌ 失败{% endif %} |
| 表数量 | {{ tables | default([]) | length }} |

---

## 表结构

{% if tables is defined and tables %}
{% for t in tables %}
- **{{ t.name }}**{% if t.comment %}（{{ t.comment }}）{% endif %}：{{ t.fields | default([]) | length }} 个字段
{% endfor %}
{% else %}
> 未记录表结构明细。
{% endif %}

---

## 执行的 DDL

```sql
{{ ddl | default('无') }}
```

{% if error %}
## 错误信息

```
{{ error }}
```
{% endif %}

---

*本报告由 goto-cloudserver-manager v0.1.0 自动生成*
